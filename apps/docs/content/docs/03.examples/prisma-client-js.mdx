---
title: Prisma Client JS
description: Integration example using Prisma ORM with PostgreSQL for type-safe database operations with `prisma-client-js`.
---

This example demonstrates using Vorsteh Queue with Prisma's `prisma-client` provider and PostgreSQL, which generates TypeScript files directly.

## Features Demonstrated

- **Traditional Prisma**: Uses `provider = "prisma-client-js"`
- **PostgreSQL Database**: Uses PostgreSQL as the database engine
- **Custom Output**: Generated client in `src/generated/prisma/`
- **JavaScript Generation**: Standard JavaScript files with TypeScript definitions
- **Email Queue**: Simple job processing with progress tracking

## Quick Start

Use the CLI to create this example:

```bash
npx create-vorsteh-queue my-app --template=prisma-client
```

## Configuration

```bash
# Edit .env with your database URL
cp .env.example .env

# generate the prisma types
pnpm prisma:generate

# push the schema to the database
pnpm prisma:push
```

## Source Code

The complete source code is available in the [examples/prisma-client](https://github.com/noxify/vorsteh-queue/tree/main/examples/prisma-client) directory.
