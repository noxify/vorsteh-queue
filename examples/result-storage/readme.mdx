---
Result Storage Example
---

Demonstrates job result storage and retrieval.
Shows how job handlers return typed results that are automatically stored and accessible through events.

## Setup

Use the CLI to create this example:

```bash
npx create-vorsteh-queue@latest my-project --template result-storage
cd my-project
pnpm dev
```
